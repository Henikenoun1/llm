POSTGRES_DB=callcenter
POSTGRES_USER=callcenter
POSTGRES_PASSWORD=callcenter
POSTGRES_PORT=5432
ADMINER_PORT=8080

CALL_CENTER_DATABASE_URL=postgresql+psycopg://callcenter:callcenter@localhost:5432/callcenter
CALL_CENTER_API_KEY=
CALL_CENTER_API_HOST=0.0.0.0
CALL_CENTER_API_PORT=8000

# OptiFlow real backend integration for tool track_order
# Preferred: set the public backend base URL, the tool endpoint will be built automatically.
OPTIFLOW_BACKEND_BASE_URL=https://5nc1c6vh-3001.euw.devtunnels.ms

# Optional explicit override for a custom tool endpoint.
# If empty, the code uses: ${OPTIFLOW_BACKEND_BASE_URL}/api/chatbot/tools/track-order
OPTIFLOW_TRACK_ORDER_URL=
OPTIFLOW_TRACK_ORDER_KEY=
OPTIFLOW_TRACK_ORDER_TIMEOUT_SECONDS=8
PIPELINE_STAGE=full
SERVE_AFTER=1
